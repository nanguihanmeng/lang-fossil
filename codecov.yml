# Codecov upload configuration (repo-side setting must be enabled first).
# Thresholds mirror the overall-coverage gate in .github/workflows/ci.yml.
comment: false
coverage:
  status:
    project:
      default:
        target: 75%
        threshold: 1%
    patch:
      default:
        target: 75%
